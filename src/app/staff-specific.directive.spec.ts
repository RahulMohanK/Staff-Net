import { StaffSpecificDirective } from './staff-specific.directive';

describe('StaffSpecificDirective', () => {
  it('should create an instance', () => {
    const directive = new StaffSpecificDirective();
    expect(directive).toBeTruthy();
  });
});
